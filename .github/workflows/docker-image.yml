name: Docker Image CI

on:
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: |
        cd hello-observability
        ./mvnw --no-transfer-progress package
        docker build . --file Dockerfile --tag hello-observability
